<Window x:Class="Tomahawk_Space.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tomahawk_Space"
        mc:Ignorable="d"
        Title="Tomahawk Space" Height="450" Width="800"
        WindowStyle="None" ResizeMode="CanResize">

    <WindowChrome.WindowChrome>
        <WindowChrome CornerRadius="1" GlassFrameThickness="1" ResizeBorderThickness="1" CaptionHeight="1"/>
    </WindowChrome.WindowChrome>
    
    <!-- Главная панель -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- Навигационная панель -->
        <DockPanel Grid.Row="0" Name="Titlebar" Background="#222" LastChildFill="False" MouseDown="Titlebar_MouseDown">
            <Button Name="Close" Click="Close_Click" Width="50" DockPanel.Dock="Right"></Button>
            <Button Name="MinMax" Click="MinMax_Click" Width="50" DockPanel.Dock="Right"></Button>
            <Button Name="Minimize" Click="Minimize_Click" Width="50" DockPanel.Dock="Right"></Button>
        </DockPanel>
    </Grid>
</Window>
